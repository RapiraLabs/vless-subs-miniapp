// ⚠️  НЕ КОММИТИТЬ В GIT — добавь config.js в .gitignore
// Скопируй из config.example.js и подставь свои значения

window.APP_CONFIG = {
  BOT_NAME: 'eblo_vpn_bot'              // ← username бота без @
};
